import 'package:flutter/material.dart';

class SeguimientoTicketPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Seguimiento'),
      ),
      body: Text('Datos incidencia')
    );
  }
}